# 宠物小屋修复方案 v1

**目标文件**：`学习台成品/新青岛版一年级学习台/pet_v2_preview.html`
**问题类型**：P0 阻塞（宠物中央大图 + 右下角小头像都破图）
**预计改动量**：5 处微改 + 约 30 行代码 / 不需要改 HTML/CSS

---

## 一、问题描述

打开学习台 → 「宠物小屋」页面，主舞台的宠物大图（id=`petImg`）和右下角浮动头像（id=`fab` 里的 img）都是破图占位（alt="pet"），其他文案/按钮/表情都正常。

## 二、根因（已核实）

| # | 位置 | 问题 |
|---|---|---|
| 1 | `pet_v2_preview.html` 第 343–348 行 `TYPES` | 只定义了 `monkey`、`dog` 两种宠物 |
| 2 | 第 386 行 `load()` | 反序列化 localStorage 时**不做字段校验**，老 type（如 `cat`）原样塞进 `S.pets[xxx].type` |
| 3 | 第 415–423 行 `imgFor(type, m)` | 直接 `TYPES[type].img`，遇到未知 type **直接抛 TypeError**（没 try/catch） |
| 4 | 第 448 行 `img.src = imgFor(type, m)` | 抛错时 `img.src` 仍是 HTML 初始的 `""` → 浏览器显示 alt 文字 |
| 5 | 第 392–394 行 `newPet(type)` / 第 894 行 `preloadFaces` | 同样假设 type 在 TYPES 里存在，领养页和老用户都会触发 |

用户浏览器 localStorage 里大概率还残留**老版（宠物板块改版前）**领养过的 `cat` 类型宠物数据。新版 `TYPES` 不认 → `imgFor` 抛错 → 破图。

中央 `<img>` 和右下角 FAB `<img>` 都走同一份 `render()` / `imgFor()`，所以同时破图。

## 三、修复方案（5 处微改）

### 修改 1：`imgFor()` 加 type 容错（核心，1 行）

**位置**：第 415–423 行

```diff
   function imgFor(type, m){
-    var t=TYPES[type].img;
+    // 容错：未知 type 自动回退到 dog（不动 TYPES）
+    if(!TYPES[type]) type = "dog";
+    var t = TYPES[type].img;
     if(m==="blink") return t.blink;
     if(m==="sleep") return t.sleep;
     if(m==="happy") return t.happy;
     if(m==="happy2") return t.happy2;
     if(m==="sad") return t.sad;
     return t.main;
   }
```

### 修改 2：`load()` 加数据迁移（启动时一次性自动清理 localStorage）

**位置**：第 385–388 行（替换原 `load()` 函数体）

```diff
   function load(){
-    try{var r=localStorage.getItem(KEY); if(r) return Object.assign(defaultState(), JSON.parse(r));}catch(e){}
+    try{
+      var r = localStorage.getItem(KEY);
+      if(r){
+        var raw = JSON.parse(r);
+        var s = Object.assign(defaultState(), raw);
+        // 迁移：把所有未知 type 回退到 dog（首选，猴子用户应已用 monkey 重养）
+        var dirty = false;
+        for(var k in s.pets){
+          if(!TYPES[s.pets[k].type]){
+            s.pets[k].type = "dog";
+            dirty = true;
+          }
+        }
+        if(dirty){
+          try{ localStorage.setItem(KEY, JSON.stringify(s)); }catch(e){}
+        }
+        return s;
+      }
+    }catch(e){}
     return defaultState();
   }
```

### 修改 3：`newPet()` 加 type 容错（领养页保护）

**位置**：第 392–394 行

```diff
   function newPet(type){
-    return {type:type, name:TYPES[type].defName, exp:0, energy:100, mood:55, lastSeen:now(), sleeping:false};
+    if(!TYPES[type]) type = "dog";
+    return {type:type, name:TYPES[type].defName, exp:0, energy:100, mood:55, lastSeen:now(), sleeping:false};
   }
```

### 修改 4：`render()` 给 `img.src` 加 try/catch 兜底（极端情况最后一道防线）

**位置**：第 447–449 行

```diff
     var img = $("petImg");
-    img.src = imgFor(type, m);
+    try{
+      img.src = imgFor(type, m);
+    }catch(e){
+      img.src = TYPES.dog.img.main;  // 兜底显示狗
+    }
     img.className = "pet-img st-"+m;
```

### 修改 5：`preloadFaces()` 加 type 容错

**位置**：第 893–897 行

```diff
   function preloadFaces(){
     var p=cur(); if(!p) return;
-    var t=TYPES[p.type].img;
+    if(!TYPES[p.type]) p.type = "dog";
+    var t = TYPES[p.type].img;
     for(var k in t){ var im=new Image(); im.src=t[k]; }
   }
```

---

## 四、用户视角验证步骤

完成上述修改后，让用户：

1. 重新打开 `学习台成品/新青岛版一年级学习台/index.html`（或你直接刷新就行）
2. 进入「宠物小屋」tab
3. 应看到：
   - 中央大图显示**狗**（老 type=cat 数据已自动迁移为 dog）
   - 右下角 FAB 头像同步显示狗
   - 宠物名字保留为原名字（如旧数据是"橘猫"会保留）
4. 用户**不需要**手动清浏览器 localStorage

如果用户想"还原" 老 cat：等下次领养页扩展 cat 时可领养一次新的。

## 五、风险评估

| 风险 | 概率 | 影响 | 缓解 |
|---|---|---|---|
| 漏改某处仍残留 cat type 触发 bug | 低 | 单点破图 | 修改 4 提供最后兜底 |
| 老用户的宠物名字被改 | 无 | — | 只动 `type` 字段，保留 `name` 不变 |
| 迁移写回 localStorage 失败 | 极低 | 下次启动重试 | try/catch 包裹 |
| 引入 monkey 类型 dog 之外的 cat/bear/... | 不可控 | 体验变更 | 不动 TYPES 列表，避免扩散 |

## 六、不在本次范围

- ❌ 不扩展新宠物（cat/bear/...）。用户当前只要 monkey + dog。
- ❌ 不动 TYPES 字段键名（避免改动 `index.html` 里可能引用的其他模块）。
- ❌ 不动样式 / 不动 HTML 结构。
- ❌ 不动 FAB 行为 / 不动其他交互。
- ❌ 不改音频 / 不改图片资源。

---

## 七、批准后我会执行

1. 用 5 个 `Edit` 工具调用按 diff 落到 `pet_v2_preview.html`
2. 自检：grep `imgFor|load|newPet|preloadFaces` 行号确认改对了
3. 写出修改摘要 + 行号清单给你
4. 同步更新 `记忆 MEMORY.md` 标"P0 已修"
